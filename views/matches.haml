.container
  .content
    .row
      %h2="Matching Jobs for #{@user.name}"
      %table{:class => "table table-striped"}
        %tr
          %th Job
          %th Missing

        - @jobs.each_with_index do |job, index|
          %tr
            %td= job[1]
            %td= @matches.to_a[index][1].collect{|skill| @skills[skill]}.compact.join(',')

